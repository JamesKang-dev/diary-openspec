## 1. 데이터 모델 보강

- [x] 1.1 저장 시 각 기록에 `id`(예: `crypto.randomUUID()`) 부여
- [x] 1.2 로드 시 `id`가 없는 기존 레코드에 인덱스 기반 임시 id 부여 (하위 호환)

## 2. 편집 상태 관리

- [x] 2.1 "현재 편집 중인 항목 id"를 추적하는 상태 변수 추가
- [x] 2.2 렌더링 함수가 편집 중인 id의 항목만 입력 필드 + 저장/취소 버튼으로 그리도록 수정

## 3. 수정 UI

- [x] 3.1 각 목록 항목에 "수정" 버튼 마크업 추가
- [x] 3.2 수정 버튼 클릭 시 해당 항목을 인라인 편집 모드로 전환 (기존 텍스트를 입력 필드 초기값으로)
- [x] 3.3 인라인 편집 모드에 저장/취소 버튼 추가

## 4. 저장/취소 동작

- [x] 4.1 저장 버튼 클릭 시 입력값이 비어있지 않으면 해당 기록의 텍스트만 갱신 (date/time/createdAt 유지)
- [x] 4.2 저장 후 localStorage에 갱신된 배열 저장, 목록 재렌더링, 편집 상태 종료
- [x] 4.3 입력값이 비어있으면 갱신하지 않고 인라인 편집 상태 유지
- [x] 4.4 취소 버튼 클릭 시 원래 텍스트로 되돌리고 편집 상태 종료

## 5. 검증

- [x] 5.1 텍스트 수정 후 저장 시 목록 순서(정렬)가 바뀌지 않는지 확인
- [x] 5.2 취소 시 원래 텍스트가 유지되는지 확인
- [x] 5.3 빈 값으로 저장 시도 시 갱신되지 않고 편집 상태가 유지되는지 확인
- [x] 5.4 새로고침 후에도 수정된 텍스트가 유지되는지 확인
