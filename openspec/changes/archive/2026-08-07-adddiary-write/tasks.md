## 1. 프로젝트 뼈대

- [x] 1.1 `index.html` 생성, Tailwind CSS 연결 (CDN 또는 빌드 방식 결정)
- [x] 1.2 `app.js`(Vanilla JS) 파일 생성 및 `index.html`에 연결

## 2. 날짜 표시

- [x] 2.1 로컬 타임존 기준으로 오늘 날짜를 `YYYY-MM-DD` 형식으로 포맷하는 함수 작성
- [x] 2.2 페이지 로드 시 날짜를 화면에 렌더링

## 3. 입력 및 저장

- [x] 3.1 한 줄 텍스트 입력 필드 + 저장 버튼 마크업 작성 (Tailwind 스타일)
- [x] 3.2 저장 버튼 클릭 핸들러 작성: 빈 입력값이면 저장하지 않음
- [x] 3.3 저장 시 `{ date, time, text, createdAt }` 레코드를 생성해 localStorage 배열에 추가 (덮어쓰지 않고 append)
- [x] 3.4 저장 후 입력 필드 초기화 및 목록 즉시 갱신

## 4. localStorage 연동

- [x] 4.1 localStorage에서 기존 기록 배열을 불러오는 함수 작성 (키 없을 때 빈 배열 처리)
- [x] 4.2 localStorage에 기록 배열을 저장하는 함수 작성
- [x] 4.3 페이지 로드 시 기존 기록을 불러와 목록 초기 렌더링에 사용

## 5. 최근 기록 목록

- [x] 5.1 기록 배열을 `createdAt` 내림차순으로 정렬하는 함수 작성
- [x] 5.2 목록 컨테이너 마크업 작성 (Tailwind로 고정 높이 + `overflow-y-auto` 적용)
- [x] 5.3 정렬된 기록을 목록 항목으로 렌더링 (날짜/시간 + 텍스트 표시)

## 6. 검증

- [x] 6.1 같은 날 여러 번 저장 시 기록이 누적되는지 확인
- [x] 6.2 새로고침 후 기존 기록이 유지되는지 확인
- [x] 6.3 기록 5개 초과 시 스크롤이 정상 동작하는지 확인
